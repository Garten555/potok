# POTOK — веб-приложение

Корневой README репозитория: [**../README.md**](../README.md) — версии, откат по тегам, структура проекта.

```bash
npm install
cp .env.example .env.local   # заполните ключи
npm run dev
```

Сборка: `npm run build` → `npm start`.
